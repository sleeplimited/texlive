\documentclass[12pt]{report}
\usepackage[letterpaper,margin=1in,heightrounded]{geometry}
%
\usepackage{tikz}
\usepackage{graphicx}
\usepackage{fouriernc}
\usepackage{fixltx2e}
%
\usetikzlibrary{calc}
%
\pagestyle{empty}
\begin{document}
\begin{center}
    \vspace*{1.3cm}
\begin{tikzpicture}[overlay,remember picture]
    \draw [line width=1pt,rounded corners=4pt]
        ($ (current page.north west) + (1.05cm,-1.05cm) $)
        rectangle
        ($ (current page.south east) + (-1.05cm,1.05cm) $);
    \draw [line width=1pt,rounded corners=4pt,
        ]
        ($ (current page.north west) + (1.25cm,-1.25cm) $)
        rectangle
        ($ (current page.south east) + (-1.25cm,1.25cm) $);
        \draw [xshift=4mm,line width=1pt,rounded corners=4pt]
        ($ (current page.north west) + (.5cm,-.5cm) $)
        rectangle
        ($ (current page.south east) + (-.5cm,.5cm) $);
    \draw [line width=1pt,rounded corners=4pt]
        ($ (current page.north west) + (1.475cm,-1.475cm) $)
        rectangle
        ($ (current page.south east) + (-1.475cm,1.475cm) $);
    \draw [line width=1pt,rounded corners=4pt]
        ($ (current page.north west) + (1.725cm,-1.725cm) $)
        rectangle
        ($ (current page.south east) + (-1.725cm,1.725cm) $);
    \draw [line width=1pt,rounded corners=4pt]
        ($ (current page.north west) + (.875cm,-.875cm) $)
        rectangle
        ($ (current page.south east) + (-.875cm,.875cm) $);
    \draw [line width=1pt,rounded corners=4pt]
        ($ (current page.north west) + (.6cm,-.6cm) $)
        rectangle
        ($ (current page.south east) + (-.6cm,.6cm) $);
    \draw [line width=1pt,rounded corners=4pt]
        ($ (current page.north west) + (.725cm,-.725cm) $)
        rectangle
        ($ (current page.south east) + (-.725cm,.725cm) $);
\end{tikzpicture}
\vfill
\textsc{Sleep Limited Publications}
\end{center}
%
\end{document}
